# Ralqlator - Command Line Calculator

## Project Overview

Ralqlator is a feature-rich command-line calculator supporting standard arithmetic, bitwise operations, multiple number formats, and mathematical functions.

> **AI-Generated Project**: This project is entirely generated by **Qwen3.5-plus** (Alibaba's large language model).

## Project Structure

```
ralqlator/
├── Cargo.toml              # Project configuration and dependencies
├── README.md               # English documentation
├── README_zh.md            # Chinese documentation
├── agent.md                # AI agent documentation (this file)
└── src/
    ├── main.rs            # Program entry point, CLI parsing and dispatch
    ├── lib.rs             # Test module (103 tests)
    ├── cli.rs             # CLI argument definitions (clap)
    ├── repl.rs            # Interactive REPL with mode switching
    ├── calculator.rs      # Core calculation orchestration
    ├── evaluator.rs       # Postfix expression evaluation
    ├── functions.rs       # Mathematical function implementations
    ├── operator.rs        # Operator/function recognition and precedence
    ├── shunting_yard.rs   # Infix to postfix conversion algorithm
    └── token.rs           # Lexical analysis (tokenization)
```

## Core Modules

### main.rs
- **Responsibility**: Program entry point, CLI parsing, mode dispatch
- **Key Functions**:
  - `main()`: Parse arguments and dispatch to calculation or interactive mode
  - `print_result()`: Format and output standard mode results
  - `print_bitwise_result()`: Format and output bitwise mode results

### cli.rs
- **Responsibility**: Command-line argument definitions
- **Arguments**:
  - `expression`: Positional argument for direct calculation
  - `-r, --row`: Expression argument (shorthand)
  - `-x, --hex`: Hexadecimal output
  - `-o, --oct`: Octal output
  - `-b, --bin`: Binary output
  - `-B, --bits`: Bitwise mode
  - `-F, --functions`: Show function help
  - `-O, --operators`: Show operator help
  - `-N, --formats`: Show number format help
  - `-C, --constants`: Show constants help

### calculator.rs
- **Responsibility**: Calculation flow orchestration
- **Functions**:
  - `calculate()`: Standard mode calculation (f64)
  - `calculate_with_functions()`: Calculation with user-defined functions
  - `calculate_bitwise()`: Bitwise mode calculation (i64)
- **Flow**: Tokenize → Resolve constants → Infix to postfix → Evaluate

### token.rs
- **Responsibility**: Lexical analysis, splitting input string into tokens
- **Supports**:
  - Scientific notation (1e3, 2.5e-3)
  - Number prefixes: 0b (binary), 0o (octal), 0x (hexadecimal)
  - Constants: pi, e
  - Bitwise operators (bitwise mode)
- **Key Functions**:
  - `tokenize()`: Main tokenization function
  - `resolve_constants()`: Resolve constants and prefixed numbers

### shunting_yard.rs
- **Responsibility**: Infix to postfix conversion (Shunting Yard Algorithm)
- **Functions**:
  - `infix_to_postfix()`: Standard mode conversion
  - `infix_to_postfix_bitwise()`: Bitwise mode conversion

### evaluator.rs
- **Responsibility**: Postfix expression evaluation
- **Functions**:
  - `eval_postfix()`: Standard mode evaluation (f64)
  - `eval_postfix_bitwise()`: Bitwise mode evaluation (i64)

### functions.rs
- **Responsibility**: Mathematical function implementations
- **Functions**:
  - `eval_factorial()`: Factorial (n!)
  - `eval_sum()`: Sum of multiple arguments
  - `eval_product()`: Product of multiple arguments
  - `eval_log_base()`: Custom base logarithm
  - `eval_lg()`: Base-10 logarithm
  - `eval_ln()`: Natural logarithm
  - `eval_sqrt()`: Square root
  - `eval_pow()`: Power function
  - `eval_sin/cos/tan()`: Trigonometric functions
  - `eval_asin/acos/atan()`: Inverse trigonometric functions
  - `eval_mod()`: Modulo function

### repl.rs
- **Responsibility**: Interactive calculator with mode switching
- **Features**:
  - Input history (arrow key navigation)
  - `@` to insert last result
  - `hex/oct/bin` commands for format conversion
  - Tab completion (shows help on empty line)
  - `mode` command to toggle standard/bitwise modes
  - `create function` for user-defined functions
  - `create sequence` for user-defined sequences
- **Structures**:
  - `run_repl()`: Standard mode
  - `run_repl_bitwise()`: Bitwise mode
  - `run_repl_with_mode()`: Unified REPL with mode switching
- **Key Commands**:
  - `Tab`: Command completion (shows help on empty line)
  - `@`: Insert last result
  - `mode`: Toggle between standard and bitwise modes
  - `mode standard/std/s`: Switch to standard mode
  - `mode bitwise/bit/b`: Switch to bitwise mode
  - `help [mode]`: Show help (optionally specify mode)
  - `operators [mode]`: Show operators for current/specified mode
  - `functions [mode]`: Show function help
  - `create function name(args) = expr`: Define custom function
  - `create sequence name(n) = expr`: Define sequence
  - `q/quit`: Exit

### operator.rs
- **Responsibility**: Operator and function recognition
- **Functions**:
  - `is_operator()`: Check if token is an operator
  - `is_bitwise_operator()`: Check if token is a bitwise operator
  - `is_function()`: Check if token is a function
  - `precedence()`: Operator precedence (standard mode)
  - `bitwise_precedence()`: Operator precedence (bitwise mode)

## Features

### Standard Operators
| Operator | Description | Precedence |
|----------|-------------|------------|
| `+` | Addition | 1 |
| `-` | Subtraction | 1 |
| `*` | Multiplication | 2 |
| `/` | Division | 2 |
| `%` | Modulo | 2 |
| `^` | Exponentiation | 3 |
| `!` | Factorial (postfix) | 4 |

### Bitwise Operators (-B mode)
| Operator | Description | Precedence |
|----------|-------------|------------|
| `\|` | OR | 1 |
| `^` | XOR | 2 |
| `&` | AND | 3 |
| `<<` | Left shift | 4 |
| `>>` | Right shift | 4 |
| `~` | NOT | 5 |

### Mathematical Functions
| Function | Description | Arguments |
|----------|-------------|-----------|
| `lg(x)` | Base-10 logarithm | 1 or 2 |
| `lg(x, base)` | Custom base logarithm | 2 |
| `log(x, base)` | Custom base logarithm | 2 |
| `ln(x)` | Natural logarithm | 1 |
| `sqrt(x)` | Square root | 1 |
| `pow(x, y)` | Power function | 2 |
| `sin(x)` | Sine | 1 |
| `cos(x)` | Cosine | 1 |
| `tan(x)` | Tangent | 1 |
| `asin(x)` | Inverse sine | 1 |
| `acos(x)` | Inverse cosine | 1 |
| `atan(x)` | Inverse tangent | 1 |
| `mod(a, b)` | Modulo function | 2 |
| `factorial(n)` | Factorial (n!) | 1 |
| `sum(a,b,...)` | Sum of arguments | Variable |
| `prod(a,b,...)` | Product of arguments | Variable |
| `suma(s,b,e)` | Sequence sum | 3 |

### Constants
- `pi` / `PI`: π ≈ 3.14159
- `e` / `E`: Euler's number ≈ 2.71828

### Number Formats
- Decimal: `123`, `-456`, `3.14`
- Binary: `0b1010`, `-0b1100`
- Octal: `0o755`, `-0o123`
- Hexadecimal: `0xFF`, `-0x1A`
- Scientific notation: `1e3`, `2.5e-3`, `1.23E+10`

## Usage Examples

### Command Line Mode
```bash
# Basic calculation
cargo run -- "1 + 2 * 3"           # Output: 7

# Using functions
cargo run -- "lg(100)"             # Output: 2
cargo run -- "sin(pi / 2)"         # Output: 1

# Bitwise operations
cargo run -- -B "12 & 10"          # Output: 8
cargo run -- -Bx "0xFF & 0x0F"     # Output: 0xF

# Format conversion
cargo run -- -x "255"              # Output: 0xFF
cargo run -- -o "255"              # Output: 0o377
cargo run -- -b "255"              # Output: 0b11111111

# View help
cargo run -- -F
cargo run -- --help
```

### Interactive Mode
```bash
cargo run
```

Interactive mode commands:
- `Tab`: Command completion (shows help on empty line)
- `q` / `quit`: Exit
- `@`: Insert last result
- `hex`: Show last result in hexadecimal
- `oct`: Show last result in octal
- `bin`: Show last result in binary
- `mode`: Toggle between standard and bitwise modes
- `mode standard`: Switch to standard mode
- `mode bitwise`: Switch to bitwise mode
- `help`: Show complete help information
- `help standard`: Show standard mode help
- `help bitwise`: Show bitwise mode help
- `operators`: Show operators for current mode
- `functions`: Show mathematical functions
- `formats`: Show number formats
- `constants`: Show mathematical constants
- `create function f(x) = x^2`: Define custom function
- `create sequence a(n) = n*(n+1)/2`: Define sequence

Bitwise interactive mode:
```bash
cargo run -- -B
```

Example session:
```
> 255
255
> hex
0xFF
> @ + 1
256
> mode
Switched to Bitwise mode (integer operations)
> 12 & 10
8
> quit
```

## Data Flow

```
Input String
    ↓
tokenize() → Vec<String>
    ↓
resolve_constants() → Vec<String>
    ↓
infix_to_postfix() → Vec<String> (postfix expression)
    ↓
eval_postfix() → Result<T, String>
    ↓
Output Result
```

## Testing

Run all tests:
```bash
cargo test
```

Test coverage (**103 test cases**):
- Basic arithmetic operations (9 tests)
- Number formats (6 tests)
- Constants (3 tests)
- Function calculations (12 tests)
- Trigonometric functions (6 tests)
- Bitwise operations (8 tests)
- Factorial function (6 tests)
- Sum/product functions (9 tests)
- Sequence sum (7 tests)
- User-defined functions (3 tests)
- Error handling (7 tests)
- Lexical analysis (6 tests)
- Parsing (3 tests)
- Output formats (3 tests)
- Edge cases (12 tests)
- Invalid input (8 tests)
- Integration tests (5 tests)

**Total: 103 test cases**

## Dependencies

```toml
[dependencies]
clap = { version = "4.4", features = ["derive"] }  # CLI argument parsing
rustyline = "14.0"                                  # Interactive input
```

## Performance Optimizations

1. **Reduced cloning**: Use `std::mem::take()` instead of `clone()` + `clear()`
2. **Pattern matching**: Use `match` instead of nested `if-else`
3. **Early returns**: Reduce unnecessary conditionals
4. **Function extraction**: Improve code reusability and readability

## Code Standards

- All code formatted with `cargo fmt`
- All code passes `cargo clippy` checks
- English comments and documentation
- Clear function naming
- Single, clear module responsibilities

## Extension Suggestions

1. **Add more functions**: Implement in `functions.rs`, register in `operator.rs`
2. **Support more bases**: Modify `parse_prefixed_number()` in `token.rs`
3. **Add variable support**: Extend `token.rs` and `evaluator.rs`
4. **Expression history**: Add history feature in `repl.rs`
5. **Custom precision**: Add floating-point precision control options

## FAQ

**Q: Why use i64 for bitwise operations?**
A: i64 provides sufficient bit width while supporting negative number bitwise operations.

**Q: How to add a new function?**
A:
1. Implement the function in `functions.rs`
2. Add the function name to `is_function()` in `operator.rs`
3. Add handling logic in `eval_postfix()` in `evaluator.rs`
4. Add documentation in `repl.rs` help information

## Author

Developed in 2026
